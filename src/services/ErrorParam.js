export const generateProductErrorParam = () => {
  const errorMessage =
    "El id no es válido. Debe ser un número entero, pero se recibió un valor invalido";
  console.log("Mensaje de error generado:", errorMessage);
  return errorMessage;
};
export const generateUserErrorParam = () => {
  const errorMessage =
    "El id no es válido. Debe ser un número entero, pero se recibió un valor invalido";
  console.log("Mensaje de error generado:", errorMessage);
  return errorMessage;
};
export const generateCartErrorParam = () => {
  const errorMessage = "no se encontro un Carrito con este valor";
  console.log("Mensaje de error generado:", errorMessage);
  return errorMessage;
};
